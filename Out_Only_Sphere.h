#ifndef OUT_ONLY_SPHERE_H
#define OUT_ONLY_SPHERE_H

#include "Container.h"
#include <fstream>

using namespace std;

//Функция вывода только шаров
void Out_Only_Sphere(Container* Head, ofstream& ofst);

#endif // OUT_ONLY_SPHERE_H